version: 0.2

phases:
  install:
    commands:
      - echo Installing...
  build:
    commands:
      - echo "Zipping build files..."
artifacts:
  files:
    - appspec.yml
    - html/**
    - scripts/**
